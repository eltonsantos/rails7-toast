RailsToastify.setup do |config|
  # config.framework = :tailwind # tailwind or :bootstrap
  config.position = 'top-right' # top-right, top-center, top-left, bottom-right, bottom-center, bottom-left
  config.animation = 'bounce' # bounce, slide, zoom, flip
  config.duration = 5000 # Duration in milliseconds
  config.theme = 'light' # light or dark
  config.type = 'error' # info, success, error, warning, default
end